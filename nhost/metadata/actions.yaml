actions:
  - name: triggerWorkflowRun
    definition:
      kind: synchronous
      handler: "{{NHOST_FUNCTIONS_URL}}/actions/trigger-workflow-run"
      arguments:
        - name: workflow_id
          type: uuid!
      output_type: WorkflowRunResult
    comment: "Triggers a workflow run. Hasura Actions only support role-level gating; org and role permissions (owner/editor check) are enforced inside the function handler itself."
    permissions:
      - role: user
  - name: approveStep
    definition:
      kind: synchronous
      handler: "{{NHOST_FUNCTIONS_URL}}/actions/approve-step"
      arguments:
        - name: step_run_id
          type: uuid!
      output_type: ApproveStepResult
    comment: "Approves a paused approval_gate step. Hasura Actions only support role-level gating; approver role verification is enforced inside the function handler itself."
    permissions:
      - role: user
  - name: createWebhookTrigger
    definition:
      kind: synchronous
      handler: "{{NHOST_FUNCTIONS_URL}}/actions/create-webhook-trigger"
      arguments:
        - name: workflow_id
          type: uuid!
      output_type: CreateWebhookTriggerResult
    comment: "Creates a webhook trigger for a workflow. Only an organization owner can create a webhook trigger. Secret is generated server-side."
    permissions:
      - role: user
custom_types:
  enums: []
  input_objects: []
  objects:
    - name: WorkflowRunResult
      fields:
        - name: run_id
          type: uuid!
        - name: status
          type: String!
    - name: ApproveStepResult
      fields:
        - name: step_run_id
          type: uuid!
        - name: status
          type: String!
        - name: resumed
          type: Boolean!
    - name: CreateWebhookTriggerResult
      fields:
        - name: trigger_id
          type: uuid!
        - name: webhook_url
          type: String!
        - name: secret
          type: String!
  scalars: []
